<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body, html {
            height: 100%;
            margin: 0;
            color: #000;
            font-family: Arial, sans-serif;
            background: linear-gradient(132deg, #fff, #88d5ed, #7fb7d6);
            background-size: 400% 400%;
            animation: Gradient 10s ease infinite;
        }

        @keyframes Gradient {
            0% {
                background-position: 0% 50%
            }
            50% {
                background-position: 100% 50%
            }
            100% {
                background-position: 0% 50%
            }
        }

        body {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .wrapper {
            display: flex;
            align-items: center;
        }

        .modal {
            background: none; /* Remove background color here */
            padding: 0; /* Remove padding */
            box-shadow: none; /* Remove box shadow */
        }

        .logo-container {
            margin-right: 2rem;
            display: flex;
            align-items: center;
        }

        .logo {
            width: 25rem;
            height: auto;
            border-radius: 50%;
        }

        .login-container {
            background-color: #11acdb;
            padding: 30px;
            border-radius: 8px;
            text-align: center;
            transition: transform 0.3s ease-in-out, background-color 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Added box shadow here */
            padding: 40px; /* Added padding here */
            border-radius: 10px; /* Added border radius here */
        }

        .login-container:hover {
            transform: scale(1.05);
            background-color: #0070ad;
        }

        h2 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #fff;
        }

        p {
            margin-bottom: 40px;
            color: #fff;
        }

        input {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        button {
            padding: 10px 20px;
            background-color: #0070ad;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #11acdb;
        }

        a {
            color: #fff;
        }

        a:hover {
            text-decoration: none;
            color: #0070ad;
        }
    </style>
</head>

<body>
    <div class="wrapper"> <!-- Added this new div to hold the logo and modal -->
        <div class="logo-container">
            <img src="/capgeniusLogo.png" alt="Capgenius Logo" class="logo">
        </div>
        <div class="modal">
            <div class="login-container">
                <h2>Sign In</h2>
                <p>Sign into Capgenius</p>
                <form action="/login" method="post">
                    <input type="text" name="username" placeholder="Username" required><br>
                    <input type="password" name="password" placeholder="Password" required><br>
                    <button type="submit">Sign In</button>
                </form>
                <p>Not a member? <a href="/signup">Sign Up</a></p>
            </div>
        </div>
    </div>
</body>
</html>
